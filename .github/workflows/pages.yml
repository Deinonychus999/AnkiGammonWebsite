name: Deploy Website to GitHub Pages

# Runs on every commit to main/master (for website updates only)
# This does NOT build the application executable - see release.yml for that
on:
  push:
    branches:
      - main
      - master
    paths:
      - 'website/**'
      - '.github/workflows/pages.yml'
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'website/**'
      - '.github/workflows/pages.yml'

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build Website
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build HTML from partials
        run: python website/build.py

      - name: Validate build output
        run: |
          echo "Validating build output..."
          test -f website/build/index.html && echo "✓ index.html exists" || exit 1
          test -d website/build/assets && echo "✓ assets directory exists" || exit 1
          echo "Validation complete!"

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    # Only deploy on pushes to main/master, not on PRs
    if: github.event_name == 'push'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build HTML from partials
        run: python website/build.py

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'website/build'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
